PROGRAM expr
BEGIN

	INT a,b,c,d,e;
	STRING newline := "\n";

	FUNCTION VOID main()
	BEGIN
			a := 1;
			b := 2;

          READ(c, d);

          e := (b*a)/a;
          c := c + a*b + (a*b+c)/a + 20;

          
          WRITE(c, newline, d, newline, e, newline);

	END	
END
